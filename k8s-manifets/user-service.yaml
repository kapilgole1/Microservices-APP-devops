apiVersion: v1
kind: Service
metadata:
  name: user
  namespace: web-app-k8s
  labels:
    app: user
spec:
  selector:
    app: user
  ports:
    - protocol: TCP
      port: 8003
      targetPort: 8003
  type: ClusterIP
