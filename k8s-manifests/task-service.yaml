apiVersion: v1
kind: Service
metadata:
  name: task
  namespace: web-app-k8s
  labels:
    app: task
spec:
  selector:
    app: task
  ports:
    - protocol: TCP
      port: 8001
      targetPort: 8001
  type: ClusterIP
